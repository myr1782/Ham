package cn.sjcloud.apm.sys.service;

import cn.sjcloud.apm.sys.page.${fileName}Page;

public interface ${fileName}ServiceIF {
	void find(${fileName}Page page);

	void findByConditon(${fileName}Page page);

	void findEntity(${fileName}Page page);

	void save(${fileName}Page page);

	void del(${fileName}Page page);
}
